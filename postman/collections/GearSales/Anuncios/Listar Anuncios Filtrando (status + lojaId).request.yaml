$kind: http-request
url: "{{baseUrl}}/anuncios?status=DISPONIVEL&lojaId={{lojaId}}"
method: GET
headers:
  Authorization: Bearer {{accessToken}}
queryParams:
  status: DISPONIVEL
  lojaId: "{{lojaId}}"
order: 3000
