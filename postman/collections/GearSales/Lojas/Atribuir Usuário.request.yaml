$kind: http-request
url: "{{baseUrl}}/lojas/{{lojaId}}/usuarios"
method: POST
headers:
  Authorization: Bearer {{accessToken}}
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "usuarioId": "{{teamMemberId}}",
      "role": "VENDEDOR"
    }
order: 6000
