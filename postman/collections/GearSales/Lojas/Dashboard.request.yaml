$kind: http-request
description: >-
  Generated from cURL: curl -X POST "{{baseUrl}}/api/lojas/dashboard"
  \
    -H "Content-Type: application/json" \
    -H "Authorization: Bearer {{accessToken}}" \
    -d '{
      "lojaId": "{{lojaId}}",
      "periodo": {
        "startDate": "2026-03-01",
        "endDate": "2026-03-31"
      }
    }'
url: "{{baseUrl}}/api/lojas/dashboard"
method: POST
headers:
  Content-Type: application/json
  Authorization: Bearer {{accessToken}}
body:
  type: json
  content: |-
    {
        "lojaId": "{{lojaId}}",
        "periodo": {
          "startDate": "2026-03-01",
          "endDate": "2026-03-31"
        }
      }
order: 12000
