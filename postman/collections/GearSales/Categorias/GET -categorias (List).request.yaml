$kind: http-request
name: GET /categorias (List)
url: "{{baseUrl}}/categorias"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('200 OK', function(){ pm.response.to.have.status(200); });
    language: text/javascript
order: 2000
