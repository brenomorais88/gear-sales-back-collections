$kind: http-request
description: |
  Generated from cURL: curl -X POST {{baseUrl}}/cambios \
    -H "Authorization: Bearer {{accessToken}}" \
    -H "Content-Type: application/json" \
    -d '{
      "nome": "Automático"
    }'
url: "{{baseUrl}}/cores"
method: POST
headers:
  Authorization: Bearer {{accessToken}}
  Content-Type: application/json
body:
  type: json
  content: |-
    {
        "nome": "Automático"
      }
order: 3000
