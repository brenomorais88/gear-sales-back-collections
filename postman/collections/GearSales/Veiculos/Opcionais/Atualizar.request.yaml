$kind: http-request
description: >
  Generated from cURL: curl -X PUT {{baseUrl}}/opcionais/UUID_OPCIONAL
  \
    -H "Authorization: Bearer {{accessToken}}" \
    -H "Content-Type: application/json" \
    -d '{
      "nome": "Vidro Elétrico",
      "tipoVeiculoId": "{{tipoVeiculoId}}"
    }'
url: "{{baseUrl}}/opcionais/{{opcionalId}}"
method: PUT
headers:
  Authorization: Bearer {{accessToken}}
  Content-Type: application/json
body:
  type: json
  content: |-
    {
        "nome": "Vidro Elétrico",
        "tipoVeiculoId": "{{tipoVeiculoId}}"
      }
order: 6000
