$kind: http-request
description: |
  Generated from cURL: curl -X POST {{baseUrl}}/opcionais \
    -H "Authorization: Bearer {{accessToken}}" \
    -H "Content-Type: application/json" \
    -d '{
      "nome": "Ar Condicionado",
      "tipoVeiculoId": "{{tipoVeiculoId}}"
    }'
url: "{{baseUrl}}/opcionais"
method: POST
headers:
  Authorization: Bearer {{accessToken}}
  Content-Type: application/json
body:
  type: json
  content: |-
    {
        "nome": "Retrovisor fotocromico",
        "tipoVeiculoId": "{{tipoVeiculoId}}"
      }
order: 5000
