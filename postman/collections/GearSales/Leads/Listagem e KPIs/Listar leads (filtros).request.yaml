$kind: http-request
url: "{{baseUrl}}/lojas/{{lojaId}}/leads?search=pedro&status=NOVO&origem=WHATSAPP&followUp=ATRASADO&onlyMine=true&page=1&pageSize=20&sortBy=OPERATIONAL&sortDescending=true"
method: GET
headers:
  Accept: application/json
queryParams:
  search: pedro
  status: NOVO
  origem: WHATSAPP
  followUp: ATRASADO
  onlyMine: "true"
  page: "1"
  pageSize: "20"
  sortBy: OPERATIONAL
  sortDescending: "true"
order: 2000
