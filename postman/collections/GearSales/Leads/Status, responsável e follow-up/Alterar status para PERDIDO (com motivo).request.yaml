$kind: http-request
url: "{{BASE_URL}}/lojas/{{LOJA_ID}}/leads/{{LEAD_ID}}/status"
method: PATCH
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: text
  content: |-
    {
      "status": "PERDIDO",
      "perdidoMotivo": "Comprou em outra loja"
    }
order: 2000
