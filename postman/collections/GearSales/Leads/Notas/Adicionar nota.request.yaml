$kind: http-request
url: "{{baseUrl}}/lojas/{{lojaId}}/leads/{{leadId}}/notes"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: text
  content: |-
    {
      "texto": "Retornar ligação amanhã"
    }
order: 2000
