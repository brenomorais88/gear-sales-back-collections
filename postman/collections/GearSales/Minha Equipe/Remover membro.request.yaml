$kind: http-request
description: Remove o vínculo do membro com a loja.
url: "{{baseUrl}}/lojas/{{selected_loja_id}}/equipe/{{usuario_id}}"
method: DELETE
headers:
  Authorization: Bearer {{accessToken}}
order: 5000
