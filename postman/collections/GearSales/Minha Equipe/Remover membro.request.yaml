$kind: http-request
description: Remove o vínculo do membro com a loja.
url: "{{baseUrl}}/lojas/{{lojaId}}/equipe/{{teamMemberId}}"
method: DELETE
headers:
  Authorization: Bearer {{accessToken}}
order: 5000
