$kind: http-request
description: |-
  Generated from cURL: curl -X POST {{baseUrl}}/modelos \
    -H "Content-Type: application/json" \
    -d '{
      "nome": "Ka",
      "marcaId": "{{brandId}}"
    }'
url: "{{baseUrl}}/modelos"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
        "nome": "Fidsdsdesta",
        "marcaId": "{{brandId}}"
      }
order: 1000
