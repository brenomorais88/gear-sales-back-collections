$kind: http-request
description: >-
  Generated from cURL: curl -X PUT
  {{baseUrl}}/modelos/00000000-0000-0000-0000-000000000002 \
    -H "Content-Type: application/json" \
    -d '{
      "nome": "Ka Sedan",
      "marcaId": "{{brandId}}"
    }'
url: "{{baseUrl}}/modelos/{{modelId}}"
method: PUT
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
        "nome": "Fusion",
        "marcaId": "{{brandId}}"
      }
order: 6000
